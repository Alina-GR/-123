function{
    Write("Введите");
    val = Int(val);
    common = Number(val);
    если (val < 0) {
    negative = ("Число отрицательное");
    }
    если (val > 0) {
    negative = ("Число положительное");
    }
    Number(num) {
    (c = 2; c < num; c++) {
    if (num % c == 0)
    }
    }
    return "Простое";
    }